# План додавання мультиплеєру

## Статус виконання
- 🟡 **В процесі** - поточний етап виконується
- ✅ **Завершено** - етап повністю виконано
- ⏸️ **Призупинено** - етап призупинено
- ⏳ **Очікує** - етап ще не розпочато

---

## Глобальні принципи

### Основні цінності проекту:
1. ✅ **Збереження якості**: Класне меню, UI/UX залишаються на високому рівні
2. ✅ **Модульність**: Кожен компонент додається незалежно
3. ✅ **Стабільність**: Проект працює після кожного етапу
4. ✅ **Чистий код**: Компонентний підхід, зрозуміла архітектура
5. ✅ **Поступовість**: Між етапами проект завжди функціональний
6. ✅ **Збереження оригіналу**: **НЕ редагуємо існуючі демо сцени**, створюємо нові для multiplayer

### Технічні принципи:
- Використання існуючих менеджерів (NetworkManager, EventBus, PlayerManager)
- RPC для синхронізації через Godot Multiplayer API
- Authority management через NetworkAuthority компонент
- Оптимізація мережевого трафіку (interpolation, prediction)
- Graceful degradation (single-player завжди працює)
- **Паралельні сцени**: Створюємо нові сцени для multiplayer (можна скопіювати існуючі), не редагуємо оригінальні

---

## Фаза 0: UI/UX Infrastructure для Multiplayer ✅

**Мета:** Створити інтерфейс для управління multiplayer сесіями без зміни основного геймплею.

### Етап 0.1: Multiplayer Menu
- [x] Створити **нову** `MultiplayerMenu` сцену (не редагувати існуючі)
- [x] Кнопки: Host Game, Join Game, Back
- [x] Створити `multiplayer_menu_controller.gd` для управління меню
- [ ] Інтеграція з існуючим меню (додати кнопку "Multiplayer" - буде в наступному етапі)
- [ ] Тестування: меню працює, не ламає single-player, оригінальні сцени незмінні

### Етап 0.2: Host Game UI
- [x] Створити **нову** `HostGameMenu` сцену (не редагувати існуючі)
- [x] Налаштування: Port, Max Players, Server Name
- [x] Кнопки: Start Server, Cancel
- [x] Інтеграція з NetworkManager.start_hosting()
- [x] Валідація введених даних
- [x] Відображення статусу (успіх/помилка)
- [ ] Тестування: можна створити хост, оригінальні сцени незмінні

### Етап 0.3: Join Game UI
- [x] Створити **нову** `JoinGameMenu` сцену (не редагувати існуючі)
- [x] Поле введення IP адреси
- [x] Поле введення Port
- [x] Кнопки: Connect, Cancel
- [x] Інтеграція з NetworkManager.join_game()
- [x] Валідація IP адреси та порту
- [x] Відображення статусу підключення
- [ ] Тестування: можна підключитися до сервера, оригінальні сцени незмінні

### Етап 0.4: Lobby UI
- [x] Створити **нову** `LobbyMenu` сцену (не редагувати існуючі)
- [x] Відображення списку гравців
- [x] Кнопка: Start Game (тільки для хоста)
- [x] Кнопка: Leave Lobby
- [x] Оновлення списку гравців в реальному часі (через PlayerManager та NetworkEventBus)
- [x] Відображення інформації про сервер (Host/Client)
- [x] Автоматичне оновлення списку гравців (Timer)
- [ ] Тестування: лобі працює, гравці бачать один одного, оригінальні сцени незмінні

### Етап 0.5: In-Game Multiplayer UI
- [x] Створити **новий** компонент `MultiplayerHUDComponent` (не редагувати існуючі)
- [x] HUD елементи для multiplayer (кількість гравців, ping, статус з'єднання)
- [x] Створити розширення для Pause Menu (`multiplayer_pause_menu_extension.gd`)
- [x] Додати кнопку "Leave Game" в меню паузи (через extension, не редагуємо оригінал)
- [x] Автоматичне приховування компонента в single-player режимі
- [ ] Інтеграція з існуючим UI (додати компоненти до сцен)
- [ ] Тестування: UI не заважає геймплею, оригінальні сцени незмінні

### Етап 0.6: World Loading State
- [x] Створити систему синхронізації завантаження сцени (`WorldLoadingManager`)
- [x] RPC для повідомлення про завантаження сцени (`_notify_peer_loaded`, `_all_peers_loaded`)
- [x] Очікування завантаження всіх клієнтів перед спавном гравців
- [x] Створити **новий** `MultiplayerLoadingScreen` (не редагувати існуючі)
- [x] Loading screen для клієнтів під час очікування
- [x] Зареєструвати `WorldLoadingManager` як автолоад
- [ ] **Створити multiplayer версії сцен** (скопіювати існуючі, не редагувати оригінали) - буде в наступному етапі
- [x] Виправлено баги: пізній конект, багаторазовий спавн, спавн хоста на клієнті
- [ ] Тестування: всі клієнти завантажують сцену перед спавном, оригінальні сцени незмінні

**Прогрес:** 3/6 етапів завершено (1.1, 1.2, 1.3 ✅)

### Етап 0.7: Пізній конект (Late Join) ⏸️ (Відкладено - потребує глибоких змін)
**Мета:** Дозволити клієнтам підключатися після початку гри.

**Статус:** ⏸️ Відкладено - для повної реалізації потрібна синхронізація стану світу, фізики та всіх об'єктів сцени. Це вимагає архітектурних змін (Фаза 2.7, 3.6).

- [x] Відстеження поточної завантаженої сцени в `WorldLoadingManager`
- [x] Автоматичне надсилання інформації про сцену новим клієнтам (`_on_network_connected`)
- [x] Завантаження сцени для пізнього клієнта (базова реалізація)
- [ ] **Повна реплікація стану сцени** - потребує синхронізації всіх об'єктів, предметів, фізики (Фаза 3.6)
- [ ] **Спавн хоста на пізньому клієнті** - потребує перегляду системи спавну (Фаза 2.7)
- [ ] **Приховування меню лобі** - потребує перегляду системи UI/сцен
- [ ] Синхронізація поточного стану світу з пізнім клієнтом (Фаза 3.6)

**Відомі проблеми:**
- Пізній клієнт не бачить хоста (хост не спавниться)
- Меню лобі не зникає на пізньому клієнті
- Не синхронізується стан світу (предмети, двері, тощо)

**Примітка:** Повна реалізація пізнього конекту потребує завершення Фаз 2.7 (архітектурні зміни) та 3.6 (синхронізація стану світу). Поки що відкладаємо на потім.

---

## Фаза 1: Player Synchronization ✅ ЗАВЕРШЕНО

**Мета:** Синхронізувати позицію, рух та базову інформацію гравців між клієнтами.

**Статус:** ✅ Основні етапи завершені (80%). Етап 1.5 (Animation Sync) відкладено як опціональний.

### Етап 1.1: Player Spawn System ✅
- [x] Створити `PlayerSpawner` для спавну гравців
- [x] Спавн локального гравця на сервері (після завантаження сцени)
- [x] Спавн віддалених гравців на клієнтах (після завантаження сцени)
- [x] Інтеграція з PlayerManager
- [x] Інтеграція з World Loading State (Фаза 0.6)
- [x] Зареєструвати `PlayerSpawner` як автолоад
- [x] Виправлено баги: багаторазовий спавн, спавн хоста на клієнті, синхронізація peer_id
- [x] Виправлено fallback-логіку в `get_player_by_peer_id` для правильної роботи на клієнті
- [ ] **Використовувати multiplayer версії сцен** (створені в Етапі 0.6) - поки використовуємо існуючі
- [x] Тестування: гравці з'являються на карті після завантаження, оригінальні сцени незмінні ✅

### Етап 1.2: Position Synchronization (ПРИОРИТЕТ) ✅
- [x] Створити `NetworkPositionSync` компонент
- [x] Додати RPC для синхронізації позиції (`_sync_position_rpc`)
- [x] Синхронізація через NetworkEventBus.player_moved
- [x] Додати компонент до CogitoPlayer динамічно (не редагуємо сцену)
- [x] Interpolation для плавного руху
- [x] Оптимізація частоти синхронізації (sync_rate, position_threshold)
- [x] Виправлено peer_id mapping для правильної синхронізації
- [x] Тестування: гравці рухаються синхронно ✅

### Етап 1.3: Rotation Synchronization (ПРИОРИТЕТ) ✅
- [x] Створити `NetworkRotationSync` компонент
- [x] Додати RPC для синхронізації обертання (`_sync_rotation_rpc`)
- [x] Синхронізація камери/голови гравця (body Y, head X)
- [x] Додати компонент до CogitoPlayer динамічно
- [x] Interpolation для плавного обертання
- [x] Обробка wrap-around для кутів (0-360 градусів)
- [x] Виправлено peer_id mapping для правильної синхронізації
- [x] Тестування: гравці обертаються синхронно ✅

### Етап 1.4: Player Visual Representation ✅
- [x] Створити `PlayerVisualRepresentation` компонент
- [x] Простий CapsuleMesh (бін як гравець)
- [x] Табличка над головою з ім'ям гравця (Label3D)
- [x] Різні кольори для різних гравців (8 кольорів)
- [x] Компонент для візуального представлення (додається динамічно)
- [x] Відображення імені гравця (Player 1, Player 2, "You" для локального)
- [x] Тестування: гравці видимі на карті з іменами ✅

### Етап 1.5: Animation Synchronization ⏸️ (Опціонально)
- [ ] Додати RPC для синхронізації анімацій
- [ ] Синхронізація стану анімації (idle, walk, run, jump)
- [ ] Тестування: анімації синхронізовані

**Примітка:** Синхронізація анімацій не критична для базового мультиплеєру. Можна додати пізніше в Фазі 2 або як окремий етап.

**Прогрес:** 4/5 етапів завершено (80%) - 1.1, 1.2, 1.3, 1.4 ✅

**Примітка:** Етапи 1.2 та 1.3 (Position/Rotation Sync) мають пріоритет над 1.4 (Visual Representation). Спочатку налаштовуємо базову синхронізацію руху з невидимими капсулами, потім додаємо візуал.

---

## Фаза 1.5: Testing Infrastructure ✅

**Мета:** Створити інфраструктуру для тестування мультиплеєру з можливістю запускати існуючі демо-сцени через хост.

### Етап 1.5.1: Scene Selection in Lobby ✅
- [x] Створити `MultiplayerSceneConfig` для конфігурації сцен
- [x] Додати вибір сцени в Lobby Menu (для хоста)
- [x] Список доступних демо-сцен (Legacy Demo, Lobby, Laboratory)
- [x] UI елементи (OptionButton, Label для опису)
- [x] Збереження вибраної сцени для завантаження
- [x] Відображення вибраної сцени в лобі (для клієнтів) - через завантаження сцени
- [x] Тестування: хост може вибрати сцену перед стартом ✅

### Етап 1.5.2: Spawn Points Setup ✅
- [x] Система спавн-пойнтів вже реалізована в PlayerSpawner (шукає групу "SpawnPoint" та connectors)
- [x] Створити `MultiplayerSpawnPoints` утиліту для додавання спавн-пойнтів динамічно
- [x] Додати спавн-пойнти після завантаження сцени (якщо їх немає)
- [x] Використовувати існуючі connectors як спавн-пойнти (fallback)
- [x] Створити кілька спавн-пойнтів на основі connectors (для кількох гравців)
- [x] Створити default spawn points якщо немає connectors
- [x] Тестування: гравці спавняться в правильних місцях ✅

### Етап 1.5.3: Multiplayer Scene Integration ✅
- [x] Використовуємо оригінальні сцени (не створюємо копії)
- [x] Спавн-пойнти створюються динамічно після завантаження сцени
- [x] Інтеграція з WorldLoadingManager для завантаження вибраної сцени
- [x] Інтеграція з LobbyMenu для вибору сцени
- [x] Тестування: можна запустити Legacy Demo через хост ✅

### Етап 1.5.4: Multi-Player Testing Setup ✅
- [x] Документація як запускати тести (хост + клієнти) - через меню Host/Join
- [x] Перевірка спавну кількох гравців одночасно ✅
- [x] Перевірка синхронізації позиції/обертання ✅
- [x] Перевірка візуального представлення ✅
- [x] Тестування: 2+ гравці можуть грати разом ✅

**Прогрес:** 4/4 етапів завершено ✅

---

## 📋 Підсумок Фази 1: Player Synchronization

### ✅ Що реалізовано:
1. **Player Spawn System** - гравці спавняться правильно на хості та клієнтах
2. **Position Synchronization** - позиції гравців синхронізуються в реальному часі
3. **Rotation Synchronization** - обертання гравців (body + head) синхронізуються
4. **Player Visual Representation** - гравці видимі як кольорові капсули з іменами
5. **Testing Infrastructure** - можна запускати демо-сцени через мультиплеєр

### ⏸️ Відкладено:
- **Animation Synchronization** - не критично для базового мультиплеєру, можна додати пізніше

### 🎯 Результат:
- ✅ 2+ гравці можуть грати разом
- ✅ Позиції та обертання синхронізуються
- ✅ Гравці видимі один одному
- 🟡 Пізній конект частково працює (потребує доопрацювання - сигнал scene_changed не відпрацьовує)
- ✅ Можна запускати існуючі демо-сцени через хост

---

## Фаза 1.6: Виправлення пізнього конекту ⏸️ (Відкладено)

**Мета:** Виправити проблему з пізнім конектом, коли клієнт підключається після початку гри.

**Статус:** ⏸️ Відкладено - потребує глибоких архітектурних змін (Фаза 2.7, 3.6).

### Проблема:
Пізній конект частково працює (сцена завантажується), але:
- Хост не спавниться на пізньому клієнті
- Меню лобі не зникає
- Не синхронізується стан світу (предмети, двері, тощо)

### Рішення (потребує архітектурних змін):
- [x] Базова реалізація завантаження сцени для пізнього клієнта
- [ ] **Повна реплікація стану сцени** (Фаза 3.6) - синхронізація всіх об'єктів, предметів, фізики
- [ ] **Перегляд системи спавну** (Фаза 2.7) - щоб хост спавнився на пізньому клієнті
- [ ] **Перегляд системи UI/сцен** - щоб меню лобі зникало правильно
- [ ] Синхронізація поточного стану світу з пізнім клієнтом (Фаза 3.6)

**Примітка:** Повна реалізація потребує завершення Фаз 2.7 (архітектурні зміни) та 3.6 (синхронізація стану світу). Поки що відкладаємо на потім.

**Прогрес:** 1/5 завдань завершено (базова реалізація)

---

## Фаза 2: Advanced Player Features ⏳

**Мета:** Синхронізувати складніші аспекти гравця (здоров'я, атрибути, стан).

### Етап 2.1: Health/Attributes Synchronization ✅
- [x] Створити NetworkAttributeSync компонент
- [x] Підключитися до сигналів attribute_changed кожного атрибута
- [x] Додати RPC для синхронізації атрибутів через NetworkManager
- [x] Оновлювати атрибути віддалених гравців через RPC
- [x] Додати компонент до CogitoPlayer динамічно
- [x] Синхронізація через NetworkEventBus.attribute_changed
- [x] Синхронізація здоров'я, витривалості тощо
- [ ] Тестування: зміни атрибутів синхронізовані

### Етап 2.2: Player State Synchronization ✅
- [x] Створити NetworkPlayerStateSync компонент
- [x] Відстежувати зміни станів (crouch, sprint, jump, тощо)
- [x] Додати RPC для синхронізації стану через NetworkManager
- [x] Оновлювати стани віддалених гравців через RPC
- [x] Додати компонент до CogitoPlayer динамічно
- [x] Синхронізація стану гравця (crouch, jump, sprint, walking, sitting, тощо)
- [ ] Тестування: стан гравця синхронізований

**Примітка:** `is_carrying` та `is_wielding` є getter'ами, тому вони не можуть бути встановлені напряму. Вони будуть синхронізовані через систему інвентаря та взаємодій в наступних фазах.

### Етап 2.3: Player Name/Identity ✅ (Перероблено на централізовану систему)
- [x] Додати систему імен гравців (зберігати в PlayerManager)
- [x] Відображення імені над гравцем (оновити PlayerVisualRepresentation)
- [x] Налаштування імені в меню (додати LineEdit в lobby_menu)
- [x] **Перероблено на централізовану систему PlayerData** - всі дані гравця зберігаються в одному місці
  - [x] Створено PlayerData клас для зберігання даних гравця (ім'я, peer_id, тощо)
  - [x] Додано RPC sync_player_data для синхронізації даних гравця
  - [x] PlayerManager зберігає PlayerData для кожного peer_id
  - [x] Автоматична синхронізація даних при підключенні нового клієнта
  - [x] PlayerVisualRepresentation підписується на player_data_updated сигнал
  - [x] Лобі меню оновлює PlayerData при зміні імені
- [ ] Тестування: імена відображаються правильно (потрібно протестувати в Godot)

### Етап 2.4: Player Death/Respawn ⏳
- [x] Синхронізація смерті гравця (базова)
- [x] Приховання HUD для віддалених гравців
- [x] Система респавну (базова, без серверної авторизації)
- [x] Інтеграція з CogitoDeathScreen (базова, без серверної авторизації)
- [ ] Тестування: смерть/респавн працює

**Прогрес:** 3/4 етапів завершено (2.1, 2.2, 2.3 ✅/🟡)

**Відомі проблеми:**
1. Зараз логіка смерті, респавну, паузи розрахована на локального гравця. Потрібно переробити для серверної авторизації (див. Фаза 2.6).
2. **"New Game" в мультиплеєрі** - при натисканні "New Game" на клієнті після смерті виникає помилка `get_player: Trying to return a previously freed instance`. Це пов'язано з оновленням сцени та потребує архітектурних змін (див. Фаза 2.7).

---

## Фаза 2.5: Централізована система синхронізації даних гравців ✅ (Завершено)

**Мета:** Переробити систему синхронізації даних гравців на централізовану архітектуру.

**Статус:** ✅ Завершено - створено централізовану систему PlayerData.

### Реалізація:
- [x] Створено PlayerData клас для зберігання всіх даних гравця (ім'я, peer_id, spawn_position)
- [x] Додано RPC sync_player_data в NetworkManager для синхронізації даних
- [x] PlayerManager зберігає PlayerData для кожного peer_id (_player_data dictionary)
- [x] Автоматична синхронізація даних при підключенні нового клієнта (хост надсилає дані всіх гравців)
- [x] PlayerVisualRepresentation підписується на player_data_updated сигнал
- [x] Лобі меню оновлює PlayerData при зміні імені та синхронізує через RPC
- [x] PlayerSpawner використовує PlayerData при спавні гравців
- [x] Додано сигнал player_data_updated в NetworkEventBus

**Переваги нової системи:**
- ✅ Всі дані гравця в одному місці (PlayerData)
- ✅ Легко додавати нові поля (просто розширити PlayerData)
- ✅ Автоматична синхронізація при підключенні
- ✅ Чиста архітектура з чіткою відповідальністю

**Прогрес:** 8/8 завдань завершено ✅

**Примітка:** Потрібно протестувати в Godot, щоб переконатися що імена відображаються правильно над усіма гравцями.

---

## Фаза 2.5.1: Виправлення проблеми з відображенням імен гравців ⏸️ (Відкладено)

**Мета:** Виправити проблему, коли імена гравців встановлюються правильно, але потім ресетяться на дефолтні значення ("Player 2", "Player 3" тощо).

**Статус:** ⏸️ Відкладено - потребує глибшого архітектурного перегляду

### Детальний опис багу:

#### Симптоми:
1. **Ім'я встановлюється правильно** - в логах видно, що ім'я синхронізується через RPC і зберігається в PlayerData
2. **Ім'я ресетиться після встановлення** - через короткий час (після ініціалізації PlayerVisualRepresentation) ім'я змінюється на дефолтне "Player 2"
3. **peer_id=-1 для remote players** - PlayerVisualRepresentation не може знайти peer_id під час ініціалізації, тому не може отримати ім'я з PlayerData

#### Причини проблеми:

1. **Race condition між реєстрацією гравця та ініціалізацією PlayerVisualRepresentation:**
   - `PlayerSpawner` реєструє гравця через `PlayerManager.register_player()`
   - `PlayerSpawner` встановлює `peer_id` через `PlayerManager.set_player_peer_id()`
   - Але `PlayerVisualRepresentation._ready()` викликається **ДО** того, як `peer_id` встановлюється
   - Результат: `peer_id=-1`, ім'я не знаходиться в PlayerData

2. **Проблема з порядком виконання:**
   - Коли гравець додається в scene tree, `_ready()` викликається автоматично
   - `PlayerVisualRepresentation._ready()` намагається отримати `peer_id` з `PlayerManager`
   - Але `set_player_peer_id()` викликається **після** додавання в scene tree
   - Результат: timing issue, `peer_id` не доступний під час ініціалізації

3. **Проблема з синхронізацією через RPC:**
   - `sync_player_data` може викликатися кілька разів
   - Якщо один з викликів містить дефолтне ім'я ("Player X"), воно може перезаписати правильне ім'я
   - `update_player_data()` не завжди перевіряє, чи нове ім'я не є дефолтним

4. **Проблема з отриманням імені:**
   - `get_player_name()` використовує `_player_names` dictionary
   - Якщо ім'я не знайдено в `_player_names`, воно повертає дефолтне "Player %d"
   - Навіть якщо ім'я є в PlayerData, воно не завжди синхронізується з `_player_names`

### Архітектурне рішення:

#### Варіант 1: Встановлення peer_id ДО додавання в scene tree (Рекомендовано)
**Переваги:**
- Найпростіше рішення
- Мінімальні зміни в коді
- Вирішує race condition

**Реалізація:**
1. Встановлювати `peer_id` на player instance через `set_meta()` **ДО** додавання в scene tree
2. `PlayerVisualRepresentation._ready()` отримує `peer_id` з `get_meta("peer_id")`
3. Якщо `peer_id` не знайдено в meta, чекати кілька кадрів і повторно спробувати з `PlayerManager`

**Недоліки:**
- Потрібно встановлювати meta на кожному player instance
- Може не працювати, якщо player instance не підтримує meta

#### Варіант 2: Використання сигналів для синхронізації (Більш надійно)
**Переваги:**
- Повністю вирішує race condition
- Чиста архітектура з подіями
- Легко розширювати

**Реалізація:**
1. Додати сигнал `player_peer_id_set(player_id, peer_id)` в `PlayerManager`
2. `PlayerVisualRepresentation` підписується на цей сигнал
3. Коли `peer_id` встановлюється, сигнал емітується
4. `PlayerVisualRepresentation` отримує сигнал і оновлює ім'я

**Недоліки:**
- Потрібно додати новий сигнал
- Потрібно переконатися, що сигнал емітується в правильний момент

#### Варіант 3: Централізована система ініціалізації (Найбільш надійно, але складніше)
**Переваги:**
- Повністю контролює порядок ініціалізації
- Немає race conditions
- Легко додавати нові компоненти

**Реалізація:**
1. Створити `PlayerInitializationManager` для управління ініціалізацією гравців
2. Послідовність:
   - Створити player instance
   - Встановити peer_id
   - Зареєструвати в PlayerManager
   - Додати в scene tree (викликає _ready() для всіх компонентів)
   - Синхронізувати дані через RPC
3. Всі компоненти отримують peer_id через `PlayerManager` після реєстрації

**Недоліки:**
- Потрібно створити новий менеджер
- Потрібно рефакторити існуючий код спавну

### Рекомендоване рішення:

**Комбінація Варіанту 1 та Варіанту 2:**
1. Встановлювати `peer_id` через `set_meta()` ДО додавання в scene tree (Варіант 1)
2. Додати сигнал `player_peer_id_set` для додаткової синхронізації (Варіант 2)
3. `PlayerVisualRepresentation` використовує обидва методи:
   - Спочатку перевіряє `get_meta("peer_id")`
   - Якщо не знайдено, підписується на сигнал
   - Коли сигнал отримано, оновлює ім'я

### Додаткові виправлення:

1. **Захист від перезапису правильного імені:**
   - `update_player_data()` не повинен перезаписувати ім'я, якщо нове ім'я є дефолтним
   - Перевірка: `if new_name.begins_with("Player ") or new_name == "You": return`

2. **Синхронізація між PlayerData та _player_names:**
   - При отриманні імені з PlayerData, автоматично оновлювати `_player_names`
   - При встановленні імені в `_player_names`, автоматично оновлювати PlayerData

3. **Логування для діагностики:**
   - Додати детальне логування всіх операцій з іменами
   - Логувати, коли ім'я встановлюється, коли воно змінюється, коли воно ресетиться

### Завдання для виправлення:

- [ ] Встановлювати `peer_id` через `set_meta()` ДО додавання player instance в scene tree
- [ ] Додати сигнал `player_peer_id_set(player_id, peer_id)` в `PlayerManager`
- [ ] Оновити `PlayerVisualRepresentation._ready()` для використання обох методів отримання `peer_id`
- [ ] Додати захист від перезапису правильного імені в `update_player_data()`
- [ ] Синхронізувати `_player_names` з PlayerData при отриманні імені
- [ ] Додати детальне логування для діагностики
- [ ] Протестувати в Godot з кількома гравцями

### Пріоритет: Середній
- Проблема не критична для функціональності (гравці все одно видно)
- Але погіршує UX (важко розрізнити гравців)
- Можна виправити після завершення більш критичних завдань

---

## Фаза 2.6: Серверна авторизація критичних дій 🔧 (Критично)

**Мета:** Переробити логіку смерті, респавну, паузи та інших критичних дій для серверної авторизації. Гра не має паузитись, ресетитись без згоди сервера.

### Проблема:
Зараз логіка смерті, респавну, паузи розрахована на локального гравця. Це означає, що:
- Гра може паузитись локально без синхронізації з сервером
- Респавн може відбуватись локально без підтвердження сервера
- Сцена може ресетитись локально без згоди сервера
- Інші критичні дії можуть виконуватись локально

### Рішення:
- [ ] **Серверна авторизація паузи**
  - [ ] Пауза має синхронізуватись з сервером
  - [ ] Тільки хост може паузити гру (або за запитом клієнта через RPC)
  - [ ] Клієнти отримують сигнал про паузу/відновлення
  - [ ] PauseMenu має працювати тільки для локального гравця, але пауза синхронізується
  
- [ ] **Серверна авторизація смерті/респавну**
  - [ ] Смерть гравця має бути підтверджена сервером
  - [ ] Респавн має ініціюватись через сервер (RPC від клієнта до хоста)
  - [ ] Хост контролює час респавну та позицію респавну
  - [ ] CogitoDeathScreen має працювати тільки для локального гравця, але респавн через сервер
  
- [ ] **Серверна авторизація ресету сцени**
  - [ ] Ресет сцени (reload_current_scene) має бути підтверджений сервером
  - [ ] Тільки хост може ресетити сцену
  - [ ] Клієнти отримують сигнал про ресет
  - [ ] WorldLoadingManager має обробляти ресет через сервер
  
- [ ] **Серверна авторизація завантаження сцени**
  - [ ] Завантаження нової сцени має бути підтверджено сервером
  - [ ] Тільки хост може ініціювати завантаження нової сцени
  - [ ] Клієнти отримують сигнал про завантаження нової сцени
  
- [ ] **Серверна авторизація виходу з гри**
  - [ ] Вихід з гри (quit) має бути оброблений правильно
  - [ ] Клієнт може вийти, але це не має впливати на інших гравців
  - [ ] Хост може вийти, але це має правильно оброблятись

- [ ] **Тестування:**
  - [ ] Пауза синхронізується між гравцями
  - [ ] Респавн працює через сервер
  - [ ] Ресет сцени працює через сервер
  - [ ] Завантаження нової сцени працює через сервер
  - [ ] Вихід з гри працює правильно

**Прогрес:** 0/5 завдань завершено

**Примітка:** Це критично важливо для стабільності мультиплеєру. Без серверної авторизації можливі десинхронізації та чити.

---

## Фаза 2.7: Архітектурні зміни для зміни локацій та мультисцен 🔧 (Критично, глибокі зміни)

**Мета:** Переробити архітектуру для підтримки зміни локацій в мультиплеєрі та можливості гравців бути на різних сценах одночасно.

### Проблема:
Зараз система розрахована на single-player, де можна просто завантажити нову сцену і забути про стару. В мультиплеєрі це не працює:
- При натисканні "New Game" на клієнті після смерті виникає помилка `get_player: Trying to return a previously freed instance`
- Зміна сцени в мультиплеєрі потребує синхронізації між усіма клієнтами
- Можливість гравців бути на різних сценах одночасно потребує нової архітектури

### Поточна ситуація:
- `CogitoDeathScreen` має кнопку "New Game", яка викликає `start_new_game()` і завантажує нову сцену
- В single-player це працює, бо гравець один
- В multiplayer це ламається, бо:
  - Клієнт намагається завантажити нову сцену локально
  - Стара сцена видаляється, але інші гравці ще на ній
  - PlayerManager намагається отримати доступ до вже видаленого гравця

### Рішення (потрібно детально прорахувати):

- [ ] **Визначити стратегію зміни локацій в мультиплеєрі**
  - [ ] Всі гравці завжди на одній сцені (простіше, але обмежено)
  - [ ] Гравці можуть бути на різних сценах (складніше, але гнучкіше)
  - [ ] Гібридний підхід (деякі сцени спільні, деякі індивідуальні)

- [ ] **Переробити систему завантаження сцен для мультиплеєру**
  - [ ] Тільки хост може ініціювати зміну сцени
  - [ ] Клієнти отримують сигнал про зміну сцени через RPC
  - [ ] Всі клієнти синхронізовано завантажують нову сцену
  - [ ] Гравці правильно респавняться на новій сцені

- [ ] **Обробка "New Game" в мультиплеєрі**
  - [ ] В multiplayer режимі "New Game" має бути відключено або працювати через сервер
  - [ ] Або замінити на "Respawn" для мультиплеєру
  - [ ] Або додати окрему логіку для "New Game" в мультиплеєрі (завантаження стартової сцени через сервер)

- [ ] **Підтримка мультисцен (якщо вибрано цей підхід)**
  - [ ] Відстеження поточної сцени для кожного гравця
  - [ ] Синхронізація переходів між сценами
  - [ ] Обробка випадків, коли гравці на різних сценах
  - [ ] UI для відображення поточної сцени гравця

- [ ] **Очищення ресурсів при зміні сцени**
  - [ ] Правильне видалення гравців зі старої сцени
  - [ ] Оновлення PlayerManager при зміні сцени
  - [ ] Уникнення помилок "previously freed instance"

- [ ] **Інтеграція з WorldLoadingManager**
  - [ ] Переконатися що WorldLoadingManager правильно обробляє зміну сцени
  - [ ] Синхронізація завантаження нової сцени
  - [ ] Правильний спавн гравців на новій сцені

- [ ] **Тестування:**
  - [ ] Зміна сцени працює в мультиплеєрі
  - [ ] "New Game" працює правильно (або відключено)
  - [ ] Гравці правильно респавняться на новій сцені
  - [ ] Немає помилок "previously freed instance"
  - [ ] Мультисцени працюють (якщо реалізовано)

**Прогрес:** 0/6 завдань завершено

**Примітка:** Це глибокі архітектурні зміни, які потребують ретельного планування. Можливо, варто спочатку реалізувати простіший підхід (всі гравці на одній сцені), а потім додати підтримку мультисцен.

**Відома помилка:** При натисканні "New Game" на клієнті після смерті: `E 0:00:33:448 get_player: Trying to return a previously freed instance.` Це через те, що клієнт намагається завантажити нову сцену локально, але гравець вже видалений.

---

## Фаза 3: Inventory Synchronization ⏳

**Мета:** Синхронізувати інвентар між клієнтами.

### Етап 3.1: Basic Inventory Sync ⏳
- [x] Додати RPC для синхронізації інвентаря
- [x] Створити NetworkInventorySync компонент
- [x] Синхронізація підняття предметів (базова)
- [x] Синхронізація скидання предметів (базова)
- [ ] Повна синхронізація інвентаря (додавання предметів до віддалених гравців)
- [ ] Спавн скинутих предметів у віддалених клієнтів
- [ ] Тестування: інвентар синхронізований

**Примітка:** Зараз реалізована базова синхронізація (логуються події підняття/скидання). Повна синхронізація потребує завантаження ресурсів предметів та додавання їх до інвентарів віддалених гравців.

### Етап 3.2: Item Usage Synchronization
 - [x] Синхронізація використання предметів (емісія подій при use_slot_data)
 - [x] Синхронізація через NetworkEventBus.inventory_item_used + NetworkInventorySync/NetworkManager
 - [ ] Тестування: використання предметів синхронізоване

### Етап 3.3: Quickslot Synchronization
- [ ] Синхронізація швидких слотів (Низький пріоритет / опціонально)
- [ ] Синхронізація через NetworkEventBus.item_equipped/unequipped (Низький пріоритет)
- [ ] Тестування: швидкі слоти синхронізовані (за потреби)

### Етап 3.4: Wieldable Synchronization
- [ ] Синхронізація зброї/інструментів
- [ ] Відображення зброї у віддалених гравців
- [ ] Синхронізація анімацій використання
- [ ] **Синхронізація стрільби/проджектайлів** (проджектайли не спавняться)
- [ ] Тестування: зброя синхронізована

**Прогрес:** 0/5 етапів завершено

**Відома проблема:** Проджектайли не спавняться в мультиплеєрі. Потрібна синхронізація спавну проджектайлів через RPC.

---

## Фаза 3.5: Виправлення синхронізації підняття предметів 🔧 (Критично)

**Мета:** Виправити проблему, коли зникають не ті предмети, які вибрав гравець (якщо на сцені багато однакових предметів).

### Проблема:
- Якщо на сцені багато однакових предметів, зникають не ті, які вибрав гравець
- ID предметів при завантаженні до сервера не синхронізуються
- Предмет зникає в клієнта, але айтем з цим ID в іншому місці був

### Рішення:
- [x] Додати передачу позиції предмета при піднятті
- [x] Додати передачу instance_id предмета при піднятті
- [x] Покращити пошук предметів: спочатку по instance_id, потім по позиції, потім fallback
- [x] Додати унікальний network_id для предметів у світі (NetworkPickupID компонент)
- [x] Автоматичне додавання NetworkPickupID до предметів при створенні
- [x] Синхронізація network_id між клієнтами
- [x] Пріоритетний пошук по network_id (найточніший метод)
- [ ] Тестування: зникають правильні предмети

**Прогрес:** 6/7 завдань завершено

**Відома проблема:** Network_id не спрацював для існуючих предметів у сцені. Потрібно переглянути архітектуру спавну та видалення предметів.

**Примітка:** Додано `NetworkPickupID` компонент, який автоматично додається до предметів з `PickupComponent` в мультиплеєрі. Хост генерує унікальний `network_id` для кожного предмета і синхронізує його з клієнтами. При пошуку предметів для видалення використовується пріоритет: scene_path > network_id > instance_id > позиція > fallback.

**Проблема:** Якщо предмети вже існують у сцені до підключення клієнта, network_id може не синхронізуватися правильно. Потрібно додати синхронізацію стану предметів при завантаженні сцени (див. Фаза 3.6).

**Відомі проблеми з instance_id:**
- `instance_id` не надійний в мультиплеєрі (локальний для кожного клієнта)
- `instance_from_id()` може викликати помилки, якщо нода вже видалена
- Потрібно поступово видалити використання `instance_id` для зворотньої сумісності (див. Фаза 3.8)

---

## Фаза 3.6: Стартова синхронізація стану сцени 🔧 (Критично, глибокі зміни)

**Мета:** Забезпечити повну реплікацію стану сцени з усіма об'єктами при завантаженні.

### Проблема:
- Зараз просто завантажується сцена в клієнта
- Не синхронізується стан об'єктів (предмети, які вже підняті, двері, які відкриті, тощо)
- Кожен клієнт має свій власний стан сцени, що може призвести до десинхронізації

### Рішення (потрібно детально прорахувати):

- [ ] **Визначити стратегію синхронізації стану**
  - [ ] Які об'єкти потрібно синхронізувати (предмети, двері, перемикачі, тощо)
  - [ ] Як зберігати стан об'єктів на сервері
  - [ ] Коли відправляти стан новим клієнтам (при завантаженні сцени, при пізньому конекті)

- [ ] **Створити систему відстеження стану об'єктів**
  - [ ] Компонент для відстеження стану об'єктів (NetworkStateSync)
  - [ ] Реєстрація об'єктів, які потребують синхронізації
  - [ ] Зберігання стану на сервері

- [ ] **Синхронізація стану при завантаженні сцени**
  - [ ] Хост збирає стан всіх об'єктів
  - [ ] Хост відправляє стан клієнтам після завантаження сцени
  - [ ] Клієнти застосовують отриманий стан до своїх об'єктів

- [ ] **Синхронізація стану при пізньому конекті**
  - [ ] Новий клієнт отримує поточний стан сцени від хоста
  - [ ] Застосування стану до вже завантаженої сцени

- [ ] **Синхронізація змін стану в реальному часі**
  - [ ] Коли об'єкт змінює стан (двері відкривається, предмет піднято), синхронізувати зміну
  - [ ] RPC для синхронізації змін стану

- [ ] **Тестування:**
  - [ ] Стан сцени синхронізується при завантаженні
  - [ ] Пізній конект отримує правильний стан
  - [ ] Зміни стану синхронізуються в реальному часі
  - [ ] Немає десинхронізації між клієнтами

**Прогрес:** 0/5 завдань завершено

**Примітка:** Це глибокі архітектурні зміни, які потребують ретельного планування. Можливо, варто спочатку реалізувати базову синхронізацію для критичних об'єктів (предмети), а потім розширити на інші.

---

## Фаза 3.8: Видалення залежності від instance_id 🔧 (Технічний борг)

**Мета:** Поступово видалити використання `instance_id` для ідентифікації предметів, оскільки він не надійний в мультиплеєрі.

### Проблема:
- `instance_id` локальний для кожного клієнта, тому не працює для синхронізації між клієнтами
- `instance_from_id()` може викликати помилки (`slot >= slot_max`), якщо нода вже видалена
- `instance_id` не зберігається між сесіями, тому не підходить для збереження стану

### Рішення (поступове видалення для зворотньої сумісності):

- [x] **Етап 1: Видалити instance_id з пріоритетів пошуку**
  - [x] Видалити `pickup_instance_id` з `_on_item_picked()` (не зберігати instance_id)
  - [x] Видалити Priority 3 (instance_id) з `_remove_pickup_from_world()`
  - [x] Оновити пріоритети: scene_path > network_id > position > fallback

- [x] **Етап 2: Видалити instance_id з серіалізації**
  - [x] Видалити `pickup_instance_id` з `item_data` в `_on_item_picked()`
  - [x] Видалити `pickup_instance_id` з `_receive_item_picked()`
  - [x] Видалити `pickup_instance_id` з `_remove_pickup_from_world()`
  - [x] Оновити логи (видалити instance_id з повідомлень)

- [ ] **Етап 3: Тестування**
  - [ ] Перевірити, що підняття предметів працює без instance_id
  - [ ] Перевірити, що видалення предметів працює без instance_id
  - [ ] Перевірити, що немає помилок `instance_from_id()`

**Прогрес:** 2/3 етапів завершено

**Примітка:** Це технічний борг. `instance_id` залишається в коді для зворотньої сумісності, але не використовується активно. Можна видалити повністю після тестування.

---

## Фаза 3.7: Перегляд архітектури спавну та видалення предметів 🔧 (Критично, глибокі зміни)

**Мета:** Переробити архітектуру спавну та видалення предметів для правильної синхронізації в мультиплеєрі.

### Проблема:
- Network_id не спрацював для існуючих предметів у сцені
- Якщо на сцені багато однакових предметів, зникають не ті, які вибрав гравець
- Предмети, які вже існують у сцені до підключення клієнта, не отримують network_id
- Синхронізація network_id може не встигнути до того, як гравець підніме предмет
- Логіка дропу зброї та вельдаблів у демо (наприклад, Foam Pistol) відрізняється від звичайних предметів

### Рішення (потрібно детально прорахувати):

- [ ] **Визначити стратегію ідентифікації предметів**
  - [ ] Використовувати шлях до ноди в сцені (для статичних предметів)
  - [ ] Використовувати унікальний ID при спавні (для динамічних предметів)
  - [ ] Комбінований підхід (шлях + позиція + network_id)

- [ ] **Переробити систему спавну предметів**
  - [ ] Хост контролює спавн всіх предметів
  - [ ] Клієнти отримують інформацію про предмети при завантаженні сцени
  - [ ] Динамічні предмети (скинуті гравцями) отримують network_id відразу

- [ ] **Переробити систему видалення предметів**
  - [ ] Використовувати точну ідентифікацію (шлях + network_id)
  - [ ] Додати перевірку перед видаленням
  - [ ] Логування для діагностики

- [ ] **Синхронізація стану предметів при завантаженні**
  - [ ] Хост збирає список всіх предметів у сцені з їх network_id
  - [ ] Хост відправляє цей список клієнтам при завантаженні сцени
  - [ ] Клієнти встановлюють network_id для відповідних предметів

- [ ] **Перегляд унікальної логіки зброї / WieldableItemPD**
  - [ ] Проаналізувати, як зараз працює дроп пістолета / зброї в синглплеєрі (`inventory_interface.gd`, `Cogito_Pistol.tres`, `pickup_pistol.tscn`)
  - [ ] Виявити відмінності від звичайних предметів (окремий `WieldableItemPD`, `wieldable_scene`, специфічний `pickup_pistol.tscn`)
  - [ ] Спроєктувати уніфікований підхід для мультиплеєра:
    - [ ] Чи зберігати окремий шлях для зброї (wieldable + pickup), але зробити його мережево-дружнім
    - [ ] Чи максимально зблизити логіку зброї з іншими предметами (єдиний шлях серіалізації + дропу)
  - [ ] Оновити `NetworkInventorySync._serialize_item()` і `_spawn_pickup_in_world()` так, щоб зброя/вельдабли:
    - [ ] Передавали коректний `drop_scene` разом з іншими даними
    - [ ] Не покладались на `resource_path` скрипта `WieldableItemPD.gd` як на єдине джерело істини
  - [ ] Додати окремі тести для:
    - [ ] Підняття/викидання пістолета між двома пірами
    - [ ] Підняття/викидання іншої зброї (лазерна гвинтівка, кирка)
    - [ ] Використання (стрільба/удари) + подальший дроп і коректний спавн у віддаленого гравця

- [ ] **Тестування:**
  - [ ] Правильні предмети зникають при піднятті
  - [ ] Працює з багатьма однаковими предметами
  - [ ] Працює з предметами, які вже були в сцені
  - [ ] Працює з динамічно скинутими предметами
  - [ ] Пістолет та інша зброя коректно спавняться/зникають у всіх гравців

**Прогрес:** 0/5 завдань завершено

**Примітка:** Це глибокі архітектурні зміни. Можливо, варто спочатку реалізувати простіший підхід - використовувати шлях до ноди в сцені для статичних предметів, а network_id тільки для динамічних.

---

## Фаза 4: World State Synchronization ⏳

**Мета:** Синхронізувати стан світу між клієнтами.

### Етап 4.1: World State Sync
- [ ] Додати RPC для синхронізації світового стану
- [ ] Синхронізація через WorldStateManager
- [ ] Синхронізація через NetworkEventBus.world_state_changed
- [ ] Тестування: зміни світу синхронізовані

### Етап 4.2: Scene Object Synchronization
- [ ] Синхронізація стану об'єктів сцени
- [ ] Синхронізація дверей, перемикачів, тощо
- [ ] Тестування: об'єкти синхронізовані

### Етап 4.3: Pickup Item Synchronization
- [ ] Синхронізація піднятих предметів
- [ ] Видалення предметів на всіх клієнтах
- [ ] Тестування: предмети синхронізовані

**Прогрес:** 0/3 етапів завершено

---

## Фаза 5: Interaction Synchronization ⏳

**Мета:** Синхронізувати взаємодії між гравцями та світом.

### Етап 5.1: Basic Interaction Sync
- [ ] Додати RPC для синхронізації взаємодій
- [ ] Синхронізація через NetworkEventBus.interaction_started/completed
- [ ] Синхронізація відкриття дверей, контейнерів
- [ ] Тестування: взаємодії синхронізовані

### Етап 5.2: Pickup Interaction Sync
- [ ] Синхронізація підняття предметів
- [ ] Authority management для предметів
- [ ] Тестування: підняття предметів синхронізоване

### Етап 5.3: Complex Interaction Sync
- [ ] Синхронізація складних взаємодій (читання, діалоги)
- [ ] Тестування: складні взаємодії синхронізовані

**Прогрес:** 0/3 етапів завершено

---

## Фаза 6: Quest Synchronization ⏳

**Мета:** Синхронізувати квести між клієнтами (опціонально, залежить від дизайну).

### Етап 6.1: Quest State Sync
- [ ] Визначити стратегію синхронізації квестів
- [ ] Індивідуальні квести vs спільні квести
- [ ] Додати RPC для синхронізації (якщо потрібно)
- [ ] Тестування: квести синхронізовані (якщо потрібно)

### Етап 6.2: Quest Progress Sync
- [ ] Синхронізація прогресу квестів
- [ ] Синхронізація через NetworkEventBus.quest_updated
- [ ] Тестування: прогрес квестів синхронізований

**Прогрес:** 0/2 етапів завершено

---

## Фаза 7: Enemy/NPC Synchronization ⏳

**Мета:** Синхронізувати ворогів та NPC між клієнтами.

### Етап 7.1: Enemy Spawn Sync
- [ ] Синхронізація спавну ворогів
- [ ] Authority management для ворогів (сервер контролює)
- [ ] Тестування: вороги з'являються синхронно

### Етап 7.2: Enemy AI Sync
- [ ] Синхронізація AI ворогів
- [ ] Синхронізація позиції, стану, цілі
- [ ] Оптимізація (тільки важливі події)
- [ ] Тестування: AI ворогів синхронізований

### Етап 7.3: Enemy Combat Sync
- [ ] Синхронізація бою з ворогами
- [ ] Синхронізація здоров'я ворогів
- [ ] Синхронізація атак/пошкоджень
- [ ] Тестування: бій синхронізований

### Етап 7.4: NPC Synchronization
- [ ] Синхронізація NPC (якщо потрібно)
- [ ] Синхронізація діалогів
- [ ] Тестування: NPC синхронізовані

**Прогрес:** 0/4 етапів завершено

---

## Фаза 8: Advanced Features ⏳

**Мета:** Додати розширені функції для покращення досвіду.

### Етап 8.1: Chat System
- [ ] Створити систему чату
- [ ] UI для чату
- [ ] RPC для повідомлень
- [ ] Тестування: чат працює

### Етап 8.2: Ping/Connection Quality
- [ ] Відображення ping
- [ ] Індикатор якості з'єднання
- [ ] Тестування: ping відображається

### Етап 8.3: Server Browser (опціонально)
- [ ] Створити браузер серверів
- [ ] Локальний пошук серверів
- [ ] Відображення списку серверів
- [ ] Тестування: браузер працює

### Етап 8.4: Save/Load in Multiplayer
- [ ] Визначити стратегію збереження в multiplayer
- [ ] Збереження стану сервера
- [ ] Завантаження стану при підключенні
- [ ] Тестування: збереження/завантаження працює

**Прогрес:** 0/4 етапів завершено

---

## Фаза 9: Optimization & Polish ⏳

**Мета:** Оптимізувати та полірувати multiplayer функціональність.

### Етап 9.1: Network Optimization
- [ ] Оптимізація частоти синхронізації
- [ ] Compression для мережевих даних
- [ ] Client-side prediction
- [ ] Lag compensation
- [ ] Тестування: мережевий трафік оптимізований

### Етап 9.2: Performance Optimization
- [ ] Оптимізація рендерингу віддалених гравців
- [ ] LOD для віддалених об'єктів
- [ ] Тестування: продуктивність покращена

### Етап 9.3: Error Handling
- [ ] Обробка помилок з'єднання
- [ ] Reconnection logic
- [ ] Graceful disconnection
- [ ] Тестування: помилки обробляються

### Етап 9.4: Testing & Bug Fixes
- [ ] Комплексне тестування
- [ ] Виправлення багів
- [ ] Тестування з різною кількістю гравців
- [ ] Тестування з різною якістю з'єднання

**Прогрес:** 0/4 етапів завершено

---

## Загальний прогрес

**Завершено:** 6/36 етапів (17%)  
**Поточний етап:** Фаза 0 завершена! Переходимо до Фази 1: Player Synchronization

---

## Нотатки та важливі моменти

### Пріоритети:
1. **Фаза 0** - Критично важлива, без неї неможливо тестувати multiplayer
2. **Фаза 1** - Базова функціональність, без неї multiplayer не працює
3. **Фаза 2-5** - Покращення досвіду
4. **Фаза 6-8** - Розширені функції
5. **Фаза 9** - Полірування

### Технічні деталі:
- Використання існуючих менеджерів (NetworkManager, EventBus, PlayerManager)
- RPC через Godot Multiplayer API
- Authority management через NetworkAuthority компонент
- Event-driven архітектура через NetworkEventBus

### Важливі принципи:
1. ✅ Кожен етап має працювати самостійно
2. ✅ Зберігати сумісність з single-player
3. ✅ Тестувати після кожного етапу
4. ✅ Документувати зміни
5. ✅ Не ламати існуючий функціонал

---

## Наступні кроки

### Поточний пріоритет (простіші завдання):
1. **Фаза 2.3 / 2.5: Виправлення синхронізації імен гравців** - простіше, не потребує архітектурних змін
2. **Фаза 3.5: Виправлення синхронізації підняття предметів** - покращення існуючої системи
3. **Фаза 3.8: Видалення залежності від instance_id** - технічний борг, вже частково виконано

### Відкладено (потребує глибоких змін):
- **Фаза 0.7 / 1.6: Пізній конект** - потребує синхронізації стану світу (Фаза 3.6) та архітектурних змін (Фаза 2.7)
- **Фаза 2.6: Серверна авторизація** - критично, але складніше
- **Фаза 2.7: Архітектурні зміни для зміни локацій** - дуже глибокі зміни

### Завершено:
- ✅ Фаза 0: UI/UX Infrastructure
- ✅ Фаза 1: Player Synchronization (основні етапи)
- ✅ Фаза 1.5: Testing Infrastructure
- ✅ Фаза 2.1, 2.2: Health/Attributes, Player State

## Важливі нотатки про порядок виконання

### Фаза 0.6 (World Loading State) - КРИТИЧНО
- **Обов'язково** виконати перед Фазою 1.1 (Player Spawn)
- Без синхронізації завантаження сцени гравці можуть спавнитися на неправильній сцені
- Гарантує, що всі клієнти завантажили рівень перед спавном

### Фаза 1.2 + 1.3 (Position/Rotation Sync) - ПРИОРИТЕТ
- **Спочатку** налаштовуємо синхронізацію руху з невидимими капсулами
- Це базова функціональність, без якої multiplayer не працює
- Після того, як рух працює синхронно, додаємо візуал (Етап 1.4)

### Фаза 1.4 (Visual Representation) - ПІСЛЯ синхронізації
- Додається **після** того, як Position/Rotation Sync працюють
- Замінюємо невидимі капсули на візуальні моделі
- Додаємо анімації та різні кольори для гравців

