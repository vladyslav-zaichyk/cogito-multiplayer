# 🗺️ Multiplayer Implementation Plan (MVP Focus)

## Статус виконання
- 🟡 **В процесі** - поточний етап виконується
- ✅ **Завершено** - етап повністю виконано
- ⏸️ **Призупинено** - етап відкладено до вирішення блокерів
- ⏳ **Очікує** - заплановано на майбутнє

---

## Глобальні принципи

### Основні цінності проекту:
1. ✅ **Збереження якості**: Класне меню, UI/UX залишаються на високому рівні
2. ✅ **Модульність**: Кожен компонент додається незалежно
3. ✅ **Стабільність**: Проект працює після кожного етапу
4. ✅ **Чистий код**: Компонентний підхід, зрозуміла архітектура
5. ✅ **Поступовість**: Між етапами проект завжди функціональний
6. ✅ **Збереження оригіналу**: **НЕ редагуємо існуючі демо сцени**, створюємо нові для multiplayer

### Технічні принципи:
- Використання існуючих менеджерів (NetworkManager, EventBus, PlayerManager)
- RPC для синхронізації через Godot Multiplayer API
- Authority management через NetworkAuthority компонент
- Оптимізація мережевого трафіку (interpolation, prediction)
- Graceful degradation (single-player завжди працює)
- **Паралельні сцени**: Створюємо нові сцени для multiplayer (можна скопіювати існуючі)

---

## 1. ✅ Реалізований Фундамент
*Ці системи вже імплементовані та працюють. Повертаємось сюди тільки для фіксу багів.*

### 1.1 UI/UX Infrastructure
- [x] **Menus:** Multiplayer Menu, Host/Join UI, Lobby зі списком гравців.
- [x] **HUD:** In-Game статус (пінг, гравці), розширене меню паузи ("Leave Game").
- [x] **Loading:** `WorldLoadingManager`, екран завантаження, синхронізація старту гри.
- [x] **Scene Config:** Вибір демо-сцени хостом перед запуском.

### 1.2 Player Core & Movement
- [x] **Spawn:** `PlayerSpawner` + динамічні спавн-пойнти.
- [x] **Movement:** Синхронізація позиції (`NetworkPositionSync`) та ротації (`NetworkRotationSync`) з інтерполяцією.
- [x] **Identity:** `PlayerData` (централізована синхронізація імен та ID), візуальні капсули з кольорами.
- [x] **Stats:** Синхронізація Health/Stamina (`NetworkAttributeSync`).
- [x] **State:** Синхронізація Crouch/Sprint/Jump (`NetworkPlayerStateSync`).

### 1.3 Server Authority Security
- [x] **Critical Actions:** Пауза, смерть, респавн, ресет сцени — тільки через сервер.
- [x] **Validation:** Клієнти не можуть викликати критичні методи напряму.

---

## 2. 🔥 Активна Фаза: MVP Gameplay
*Мета: Коректна робота предметів та базової взаємодії. Без цього в гру неможливо грати.*

### 2.1 Inventory & Items Fix (Пріоритет №1) 🚧
*Вирішення проблеми: "Зникають не ті предмети" та "Предмети дублюються".*

**Оцінка прогресу: 80% завершено** ✅

- [x] Створити `NetworkPickupID` для унікальної ідентифікації. ✅
- [x] Синхронізація видалення через пріоритет (`network_id` > позиція). ✅
- [x] **Refactor:** Переробити систему спавну дропу (щоб викинутий предмет мав ID). ✅ (Додано NetworkPickupID до спавнених предметів)
- [x] **Fix:** Забезпечити генерацію `network_id` для предметів, що вже існують на сцені (Pre-placed items). ✅ (Додано автоматичну ініціалізацію в WorldLoadingManager)
- [x] **Tech Debt:** Повністю видалити залежність від `instance_id` (ненадійний). ✅ (Видалено з NetworkInventorySync, залишились тільки коментарі)

**Детальна оцінка:**

✅ **Що вже працює:**
- `NetworkPickupID` компонент створено і працює
- Синхронізація видалення через пріоритет реалізована (`network_id` > `scene_path` > позиція)
- Спавн дропу: викинуті предмети автоматично отримують `NetworkPickupID`
- Pre-placed items: автоматично отримують `NetworkPickupID` при завантаженні сцени (на хості)
- `instance_id` видалено з `NetworkInventorySync` (залишились тільки коментарі)
- Виправлено завантаження сцени для клієнтів (дозволено через WorldLoadingManager)

**Статус:** ✅ **80% завершено** - основна функціональність працює. Залишилось тільки тестування та можливі дрібні покращення.

### 2.2 Wieldables Sync (Зброя в руках) ⏸️
*Мета: Інші гравці бачать, що я тримаю (пістолет, ліхтарик).*

**Статус:** ⏸️ **Відкладено** - Базова синхронізація працює, але орієнтація неправильна. Потребує архітектурного виправлення в майбутньому.

**Оцінка прогресу: 60% завершено** ✅

**Що працює:**
- ✅ `NetworkWieldableSync` компонент створено і працює
- ✅ Базова синхронізація екіпірування/зняття зброї через RPC
- ✅ Відстеження змін через сигнал `updated_wieldable_data`
- ✅ Візуальний спавн моделі зброї в руках віддаленого гравця
- ✅ Синхронізація стану ліхтарика (вкл/викл) через RPC `sync_flashlight_state`
- ✅ Wieldables ховаються при unequip

**Відомі проблеми (відкладено до ViewModel/WorldModel архітектури):**
- ⚠️ Орієнтація wieldables неправильна (180° поворот) - спроби виправити через `equip()` та ручну корекцію не спрацювали
- ⚠️ Wieldables можуть не повністю слідувати за рухом камери (тільки одна вісь)
- ⚠️ Поточна реалізація використовує ViewModel (FPS-камеру) для віддалених гравців, що не є ідеальним рішенням

**TODO для майбутнього:**
- 🔴 **Архітектурне виправлення:** Реалізувати ViewModel/WorldModel архітектуру (див. `VIEWMODEL_WORLDMODEL_PLAN.md` та Phase 2.4)
- 🔴 Розділення ViewModel (локальний гравець) та WorldModel (віддалені гравці)
- 🔴 Синхронізація тільки ID предмета, а не Transform
- 🔴 Створення Hand Socket в PlayerVisualRepresentation
- 🔴 WorldModelWieldableSync компонент для віддалених гравців

**Примітка:** Поточна реалізація працює для MVP (wieldables видно, але орієнтація неправильна). Повна архітектурна переробка запланована на Phase 2.4. Спроби виправити орієнтацію через `equip()` та ручну корекцію не спрацювали, тому відкладено до реалізації правильної архітектури.

### 2.3 World Interaction (Basic) ✅
*Мета: Двері та кнопки працюють синхронно.*

**Оцінка прогресу: 100% завершено** ✅

- [x] Створити універсальний `NetworkInteractable` компонент. ✅
- [x] Синхронізація `CogitoDoor` (відкрито/закрито). ✅
- [x] Синхронізація `CogitoSwitch` (вкл/викл). ✅
- [x] Синхронізація початкового стану (при вході на сервер). ✅

**Що реалізовано:**
- ✅ `NetworkInteractable` компонент створено і додано до дверей та перемикачів автоматично
- ✅ Синхронізація стану дверей (відкрито/закрито, заблоковано/розблоковано) через RPC
- ✅ Синхронізація стану перемикачів (вкл/викл) через RPC
- ✅ Автоматичне додавання компонента до всіх дверей та перемикачів при завантаженні сцени (на хості)
- ✅ Синхронізація початкового стану при завантаженні сцени
- ✅ RPC `sync_interactable_state` додано в NetworkManager
- ✅ Правильна обробка різних типів дверей (ROTATING, SLIDING, ANIMATED)

**Статус:** ✅ **100% завершено** - основна функціональність працює. Готово до тестування.

### 2.5 Projectile Sync (Зброя та проджектайли) 🚧
*Мета: Проджектайли (кулі, дротики) синхронізуються між гравцями.*

**Оцінка прогресу: 80% завершено** ✅

- [x] Додати RPC `sync_projectile_spawn` в NetworkManager. ✅
- [x] Модифікувати `wieldable_toy_pistol` для надсилання RPC при стрільбі. ✅
- [x] Модифікувати `wieldable_throwable` для надсилання RPC при киданні. ✅
- [ ] Синхронізація ушкоджень від проджектайлів (опціонально, якщо потрібна серверна авторизація). ⏳

**Що реалізовано:**
- ✅ RPC `sync_projectile_spawn` додано в NetworkManager
- ✅ Синхронізація спавну проджектайлів (позиція, ротація, швидкість, ушкодження)
- ✅ Підтримка `wieldable_toy_pistol` (пістолет)
- ✅ Підтримка `wieldable_throwable` (кидальні предмети)
- ✅ Автоматичне встановлення `damage_amount` та `Direction` для проджектайлів
- ✅ Перевірка, що RPC надсилається тільки від локального гравця

**Що залишилось:**
- ⏳ Синхронізація ушкоджень (якщо потрібна серверна авторизація для запобігання читерству)

**Статус:** ✅ **80% завершено** - основна функціональність працює. Проджектайли синхронізуються між гравцями. Готово до тестування.

### 2.4 ViewModel/WorldModel Architecture 🔴
*Мета: Правильна архітектура для відображення зброї в мультиплеєрі.*

**Детальний план:** Див. `VIEWMODEL_WORLDMODEL_PLAN.md`

**Короткий опис:**
- Розділення ViewModel (локальний гравець) та WorldModel (віддалені гравці)
- Синхронізація тільки ID предмета, не Transform
- Hand Socket в PlayerVisualRepresentation для WorldModel
- WorldModelWieldableSync компонент для віддалених гравців

**Пріоритет:** 🔴 Високий - Фундаментальна архітектурна зміна, яка вирішить поточні проблеми з wieldables sync.

**Оцінка часу:** ~6-9 годин для MVP реалізації

**Залежності:**
- Phase 2.2 Wieldables Sync (поточна реалізація) - потрібно замінити
- PlayerVisualRepresentation - потрібно розширити
- WieldableItemPD - потрібно додати поле `world_model_scene`
- PlayerData - потрібно додати поле `current_wielded_item_id`

---

## 3. 🏗️ Фаза: Архітектура Світу (Complex)
*Вирішення складних проблем: Пізній конект, Зміна рівнів, Збереження стану.*

### 3.1 World State Manager (Replication)
*Об'єднує колишні задачі по "Late Join" та "Scene Objects".*
- [ ] Створити `WorldStateManager` для збереження глобального стану.
- [ ] Реєстр змін: які двері відкриті, які предмети забрали.
- [ ] **Late Join:** При підключенні нового гравця надсилати йому актуальний пакет стану.

### 3.2 Scene Travel System
*Вирішення проблеми з "New Game" та крашами при зміні карти.*
- [ ] Реалізувати "Server Travel" (Хост міняє карту -> Всі летять за ним).
- [ ] Коректне очищення `PlayerManager` перед завантаженням нової сцени.
- [ ] Заміна кнопки "New Game" на "Respawn" або серверний ресет рівня.

---

## 4. 🐛 Відомі Баги (Active Issues)
1. **Player Names:** Іноді ім'я ресетиться на "Player 2" (Race condition при ініціалізації). *Потрібен фікс у `PlayerVisualRepresentation`.* ⏸️ Відкладено
2. **Weapons:** Зброя та кулі не відображаються у інших гравців. *Планується в Фазі 2.2*
3. ~~**Pre-placed Items:** Предмети, розставлені в редакторі, не мають синхронізованих ID, тому некоректно піднімаються клієнтами.~~ ✅ **Виправлено** - додано автоматичну ініціалізацію NetworkPickupID

---

## 5. 💤 Backlog / Post-MVP (Відкладено)
*Задачі низького пріоритету. Робити тільки після стабілізації MVP.*

### 5.1 Visual Polish
- [ ] **Animations:** Синхронізація BlendTree (ходити/бігати) та AnimationPlayer.
- [ ] **Interpolation Tuning:** Згладжування руху при поганому пінгу.

### 5.2 Advanced Gameplay
- [ ] **Complex Inventory:** Передача вмісту рюкзака, трейд, контейнери (скрині).
- [ ] **Physics:** Синхронізація RigidBody (коробки, які можна штовхати).
- [ ] **Quests:** Синхронізація прогресу квестів.
- [ ] **AI:** Синхронізація ворогів (Spawn, Aggro, Death).

### 5.3 Social Features
- [ ] Chat System.
- [ ] Server Browser.
